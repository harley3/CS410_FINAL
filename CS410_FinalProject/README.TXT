Copy the following files to server w/ PHP:

queryForm.php
viewDoc.php
QueryManager.jar

Copy the following Document Collection folder to server:

./input

RUN COMMAND-LINE TEST(query keyword: Britain):

java -jar QueryManager.jar Britain